# 企业AI助手（Enterprise AI Assistant）

> 多 Agent 集群：一个统一问答入口，一个调度器，多个专业 Agent
> 用户只提问，不用关心"该找哪个助手"——由 AI 助手判断意图、分发给合适的 Agent，必要时组合多个 Agent 回答

## Agent 列表

| 目录 | Agent | 职责 | 状态 |
|---|---|---|---|
| `ai-assistant` | 统一 AI 助手（调度器） | 意图识别、路由分发、结果汇总、调用统计；聊天端 + 管理端两个前端 | 运行中（v0.2） |
| `kb-agent` | 企业知识问答 | 制度查询、文档问答、流程助手（RAG） | 运行中（v0.2） |
| `doc-review-agent` | 企业项目文档审查 | 审查合同、方案、报告，指出问题与风险 | 规划中 |
| `data-analysis-agent` | 企业数据分析 | 基于业务数据查询、统计、分析与归纳 | 规划中 |
| `notice-agent` | 企业通知助手 | 通知起草、优化、发布与送达管理 | 规划中 |
| `study-agent` | 研读库（个人向） | 论文/课程的精读·泛读笔记、阅读记录与统计（面向本人的论文写作练习） | 建设中：前端已搭好（静态数据），后端骨架 |
| `archived` | 早期项目归档 | tricore 业务系统等（仅纪念留存） | 已归档 |

## 统一调度设计

### 目标架构

```
前端（聊天端 frontend + 管理端 admin-frontend，均属于 ai-assistant 服务）
    ↓
ai-assistant（统一AI助手服务：调度器 + 统一问答接口）
    ├→ kb-agent             企业知识问答
    ├→ doc-review-agent     企业项目文档审查
    ├→ data-analysis-agent  企业数据分析
    └→ notice-agent         企业通知助手
```

- 用户只面对一个问答入口，由 AI 助手判断意图、分发、汇总
- 各 Agent 是独立服务，只通过统一协议与调度器通信

### 实施路径（2026-08-25 修订）

直接按最终形态建设：ai-assistant 从第一天就是独立调度服务，kb-agent 作为第一个被接入的 Agent，前端（Vite）属于 ai-assistant 服务。分步构建计划见 `ai-assistant/README.md`。

> 原方案（调度先内嵌 kb-agent，成熟后再拆出）已放弃——改为直接建独立服务，避免中途迁移。

### Agent 统一协议

每个 Agent 只暴露一个标准接口（内部随便实现）：

```
输入：{"question": "...", "context": {...}}
输出：{"answer": "...", "references": [...], "confidence": 0.9}
```

调度器只认这套协议，换 Agent、加 Agent 都不用改调度器。
内嵌阶段它是进程内函数调用的参数/返回值；拆分阶段它就是 HTTP 请求/响应体——形态不变。

### 调度器选 Agent

- LLM 意图识别输出 JSON（复用 classify_upload 模式）：`{"agent": "knowledge", "reason": "..."}`
- 识别失败/低置信 → 降级 knowledge
- 可扩展为 `{"agents": ["data", "knowledge"]}` 表示多 Agent 组合

### 多 Agent 组合

- 第一阶段：一个主 Agent + 可选辅助 Agent，并行调用，主 Agent 汇总
- 第二阶段（进阶）：串行依赖（A 的结果喂给 B）

### 第二个 Agent 选型

推荐先做 **doc-review-agent（文档审查）**：上传合同/方案/报告 → LLM 审查风险、给修改建议。复用现有文件解析能力，不依赖外部系统，能最快验证多 Agent 路由。（待确认）

## 统一服务管理（manager）设计

> 服务多了以后，每个服务占一个终端、状态靠肉眼盯不可持续——由 manager 守护服务统一管理各服务的启动、运行状态、自动重启、停止与日志

### 架构

```
你手动启动的（只有一个）
        │
┌─────────────────────────────────────────────┐
│  manager（守护服务，8002）                    │
│   起停控制 + 探活 + 自动重启 + 日志落文件      │
└──────┬──────────────┬───────────────┬───────┘
       │ 拉起/监控     │               │
┌──────▼──────┐  ┌────▼────────┐  ┌───▼─────────────┐
│ kb-agent    │  │ ai-assistant│  │ 前端（Vite dev）  │
│ 8000  RAG   │  │ 8001  调度器 │  │ 聊天 5173/管理 5174│
└─────────────┘  └─────────────┘  └─────────────────┘

管理端 5174（服务管理页）
     ├→ 8001 的 /api/v1/admin    ← 业务数据（统计、kb 管理，已有）
     └→ 8002 的 /api/v1/services ← 进程控制（新增，直连不代理——
                                    ai-assistant 挂了还得能重启它）
```

### 关键决策

- **manager 是根进程**：唯一手动启动的服务，不被任何被管服务托管；它自己的常驻守护后续交操作系统（Linux systemd / Windows 任务计划），现阶段挂了手动拉起
- **进程控制直连 8002**：不走 8001 代理（否则 ai-assistant 挂了无法重启它）
- **跨平台**：启动命令用 `{python}` / `{npm}` 占位符（manager 按系统解析为 `.venv\Scripts\python.exe`、`npm.cmd` 等真实路径），换操作系统 = 改配置不改代码；上 Linux 服务器后与 systemd 互补（systemd 管兜底，manager 管统一界面）
- **探活**：httpx 请求各服务的探活路径（配置项 `probe_path`——后端 FastAPI 用 `/docs` 零改动，Vite 前端用 `/`）
- **自动重启**：监控循环周期探活，进程死了自动拉起并计数；连续失败 5 次转 error 停止尝试（防端口冲突死循环）；手动 stop 的不自动拉起
- **服务清单即配置**（`manager/services.json`）：名称/端口/目录/命令/探活路径/是否随 manager 启动，新 Agent 接入加一行即可

### 管理范围（2026-09-10 扩大至四个进程）

kb-agent、ai-assistant、聊天端 5173、管理端 5174（Vite dev server 配 `probe_path: "/"`；`vite.config.js` 加 `strictPort: true`——端口被占直接报错，不自动换端口，否则 manager 按固定端口探活会"失联"；`host: '127.0.0.1'`——Vite 默认只绑 IPv6 的 localhost（[::1]），manager 探 IPv4 的 127.0.0.1 会连不上，实测踩坑后固定 IPv4 与后端一致）；生产环境前端（nginx）同为进程，照配置加一条即可。

## 快速启动

> 端口约定：kb-agent 8000、ai-assistant 8001、manager 8002、聊天端 5173、管理端 5174、研读端 5175（研读库后端 8003）；浏览器访问聊天端 http://localhost:5173、管理端 http://localhost:5174、研读端 http://localhost:5175

### 1. 依赖安装（首次，每个项目各装一次）

```powershell
# kb-agent（Python）
cd kb-agent
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# ai-assistant（Python，另开一个终端）
cd ai-assistant
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 前端（Vite + Vue3，聊天端）
cd ai-assistant\frontend
npm install

# 管理端前端（Vite + Vue3 + daisyUI，Agent 管理界面）
cd ai-assistant\admin-frontend
npm install
```

### 2. 配置（首次）

kb-agent 和 ai-assistant 各有一份 `.env.example`，复制成 `.env` 并按注释填入密钥（两个项目的 LLM 配置相同；kb-agent 还需要 Embedding 配置，见它的 .env.example 注释）。

### 3. 启动（日常）

**方式 A（推荐）：manager 一键拉起全部服务**

```powershell
cd manager
.venv\Scripts\python.exe -m uvicorn main:app --port 8002
```

kb-agent、ai-assistant 与两个前端（5173/5174）都会被自动拉起并持续守护（死了自动重启），日常只需手动启动 manager 一个进程；详见「统一服务管理（manager）设计」。（研读库 study-agent 开发中，暂独立运行，后端就绪后再接入 manager。）

**方式 B：逐个手动启动（四个终端各跑一个）**

```powershell
# 终端 1：kb-agent（企业知识问答）
cd kb-agent
.venv\Scripts\python.exe -m uvicorn main:app --port 8000

# 终端 2：ai-assistant（统一调度器）
cd ai-assistant
.venv\Scripts\python.exe -m uvicorn main:app --port 8001

# 终端 3：前端（聊天界面）
cd ai-assistant\frontend
npm run dev

# 终端 4：管理端前端（Agent 管理界面，可选）
cd ai-assistant\admin-frontend
npm run dev
```

kb-agent 没启动时 ai-assistant 也能用（知识问题自动降级为通用对话）。

## 更新日志

> 历史备注：早期项目（tricore 系列：销售/库存/办公协同业务系统）已于 2026-08-25 归档至 `archived/`，不再维护。

#### 2026年09月10日【manager 扩展四进程管理 + 研读库（study-agent）立项与前端搭建】

- **manager 扩展：前端服务纳入统一管理（四进程）**：`services.json` 新增 frontend（5173）/ admin-frontend（5174）；新增 `{npm}` 占位符（Windows 上是 npm.cmd，用 which 取真实路径）；命令拼接改为"先拆 token 再替换占位符"（解析出的路径含空格也不会被拆坏）；新增 `probe_path` 探活路径配置（后端 `/docs`，Vite 用 `/`）
- **Vite 固定绑定踩坑**：Vite 默认只绑 IPv6 的 localhost（`[::1]`），manager 按 IPv4 探活连不上 → 判死并反复重启；修复：`vite.config.js` 加 `host: '127.0.0.1'` + `strictPort: true`（端口被占直接报错，不自动漂移）
- **日志乱码修复**：Vite 输出重定向到文件后仍写 ANSI 颜色码，管理页 `<pre>` 里显示成乱码 → manager 读取日志时清除 ANSI；Python 子进程输出默认按系统 GBK 编码、与 Node 的 UTF-8 混写 → spawn 时统一 `PYTHONIOENCODING=utf-8`
- **研读库立项（`study-agent/`，个人向）**：精读 + 泛读论文/课程的结构化笔记与阅读管理（服务于毕业论文写作练习）；独立成项目而非 kb-agent 加类型——数据模型本质不同（结构化阅读管理 vs 文档分块问答），未来可接入调度器成为集群 Agent
- **前端已搭好（5175，静态数据）**：Vite 8 + Vue3 + daisyUI + pinia + axios + vue-router（与 admin-frontend 同版本）。研读页：左上传占位（PDF/图片下一步接入）+ 右笔记（CodeMirror 6 Markdown 编辑器：实时语法着色、Tab 缩进）+ 选填文献行（作者/出版时间/期刊）+ 底部实时时钟（年月日/星期/秒）+ 泛读模式左栏收起、笔记居中的过渡动画。回顾页：搜索 + 精读/泛读筛选（左栏、吸顶跟随）+ 右下角悬停展开的统计卡（条目/精读泛读/完成度进度条/累计与平均字数/本月新增/最近更新）。详情页：Markdown 渲染（marked）
- **数据层静态模拟先行**：`frontend/src/api/readings.js` 返回与真实接口同形状的静态数据（`{message, data}` + 字段一致），后端就绪后只改这一个文件（用户拍板顺序：先定前端 → 再定接口 → 再定表）；记录字段约定：title / mode（close,skim）/ status（draft,done）/ author / published / journal / note / 时间戳
- **后端骨架（8003）**：应用入口 + SQLite 配置 + readings 模型已建；接口留到"构建后端"阶段
- **出版时间控件探索记录**：原生 month → Vanilla Calendar Pro → Cally（选年月不便）→ 回 VCPro → 最终定为普通文本框手填——结论：个人工具优先简单可靠的输入方式

#### 2026年09月03日【企业AI助手·manager 守护服务与运维管理页】

- **manager 服务建成**（8002，新目录 `manager/`）：项目唯一手动启动的根进程——按 `services.json` 配置拉起各服务（subprocess + 各服务自己 venv 的 `{python}` 占位符跨平台解析）、httpx 探活 `/docs`（现有服务零改动）、5 秒监控循环自动重启（连续失败 5 次转 error 防崩溃死循环）、手动 stop 的不被拉起、日志统一落 `manager/logs/`
- **管理端新增「服务管理」页**：状态灯 / 运行时长 / 重启次数 + 起停/重启按钮 + 日志弹窗，5 秒轮询；进程控制走新代理 `/ops` → 8002 **直连**（不经 8001——ai-assistant 挂了还得能重启它）
- 联调通过：manager 启动自动拉起两个服务；taskkill 杀掉 kb-agent 后 5 秒内自动重启（restarts 计数）；手动停止保持 stopped 不被拉起；日志接口正常；vite 代理链路验证通过
- 设计要点记录于 README「统一服务管理（manager）设计」：根进程约束（manager 不能被被管服务托管）、跨平台配置、自守护问题（manager 自己的常驻后续交 systemd/任务计划，上 Linux 服务器后与 systemd 互补）

#### 2026年09月01日【企业AI助手·管理界面v1：调用统计与kb管理页】

- **前端拆分为两个项目**：聊天端（`frontend/`，5173，全体员工用）与管理端（`admin-frontend/`，5174，管理员用）分离——受众、权限、部署范围不同，各自独立演进；两者都只走 8001 代理
- **admin-frontend 建成**（Vite 8 + Vue 3.5 + vue-router 5 + pinia 4 + Tailwind 4.3 + daisyUI 5.7 + lucide 图标）：侧边栏布局 + 4 条懒加载路由（总览 / 上传知识 / 知识列表 / 未命中问题）；Tailwind v4 CSS-first 配置（无 tailwind.config.js，style.css 里 @plugin daisyui）
- **ai-assistant 建库（它的第一个数据库）**：SQLite assistant.db + calls 表——每次问答调用记一笔（意图 / 实际回答来源 / 是否降级 / 耗时），只有调度器有全链路视角；流式接口用生成器外层 try/finally 保证断流也落库
- **管理接口 `/api/v1/admin`**：`/overview`（各 Agent 在线状态 + 调用统计：总次数 / 知识问答成功率 / 降级次数 / 平均耗时）、`/calls`（最近调用记录）；kb-agent 数据走代理模式（前端 → 8001 → 8000）：知识列表 / 删除 / 上传（multipart 透传）、未命中列表 / 删除 / 清空，kb-agent 不可用统一返回 502
- 联调通过：真实问答成功记录调用（7177ms，无降级）；上传 → AI 识别分类 → 删除全链路验证，测试数据已清理
- 补充说明：管理端「知识问答成功率」= kb-agent 接住数 / 知识类问题数；Agent 清单（含规划中）目前是硬编码列表，新 Agent 接入时再接到 registry

#### 2026年08月31日【企业知识库Agent·检索质量与未命中记录】

- **回答机制讨论**：评估宽度/深度分流——结论保留（枚举/概览类问题 RAG 天生答不了，移除会重现"只答出两个制度"的问题）；宽度机制触发场景窄但成本低，像保险
- **检索质量改进（分数阈值 + 动态 k）**：先标定（`debug_score_distribution.py` 实测 bge-m3 分数分布）——强命中 0.57~0.74，弱命中 ~0.50，但库里没有的问题也能到 0.64，**绝对阈值区分不了命中和未命中**；设计定为：阈值 0.5 只砍明显垃圾，动态取块（上限 6，命中块多回答更丰富），真正的命中判断交给 LLM 诚实回答机制
- **未命中问题记录（missed_questions 表）**：知识库答不上的问题自动入库——用户问什么没答上，就是该补什么知识；两个触发点（检索零命中 / LLM 回复"暂无相关内容"，后者更可靠）；重复提问累计次数；新增管理接口 `GET/DELETE /api/v1/missed`（按次数排序，补完知识后删除）
- 讨论记录：长文档（几十MB）存储——SQLite TEXT 装得下但该存磁盘、瓶颈在 embedding 成本不在存储；向量库（Milvus 等）当前规模不需要（NumPy 全量点积够用，README 已预留迁移路径）；批量上传三方案对比（前端并发 / 后端批量接口 / 任务队列），推荐先做前端并发
- 未命中回复文案统一：固定话术与 LLM 诚实回复两处统一为"知识库中暂无相关内容，该问题已记录，我们会尽快补充相关资料。"（此前 LLM 路径只回复短短语、缺少记录告知，已修复）
- **Agent 管理界面方案确认（下一步）**：管理入口放调度器前端（侧边栏：聊天 / Agent 管理）——问答一个入口、管理也一个入口；调用情况由 ai-assistant 新增 `calls` 表记录（意图/目标Agent/耗时/降级，它的第一个数据库）；数据信息走代理模式（前端 → 8001 → 各 Agent 管理接口，不直连）；先不抽象统一管理协议，先把 kb-agent 页面做实（上传/知识列表/未命中清单）

#### 2026年08月26日【企业AI助手·调度器与前端 + 知识库宽深度问答】

- **ai-assistant 调度服务建成**（8001 端口）：六步计划全部完成——服务骨架、LLM 服务层（意图识别 JSON 输出 + 通用对话兜底）、统一问答接口 `/api/v1/chat` 与 `/api/v1/chat/stream`（SSE）、Agent 注册表 + kb-agent 适配器（httpx 调用 + SSE 原样透传）
  - 路由规则：knowledge 且 kb-agent 在线 → 转发；kb-agent 不可用 → 降级兜底（`degraded: true`），入口永不中断
  - 兜底对话不落库，由前端传 history；kb-agent 多轮记忆仍走它的 session_id
- **前端建成**（Vite + Vue3，`ai-assistant/frontend/`，5173 端口）：聊天界面 + SSE 流式渲染 + markdown + 来源展示；Vite 代理 `/api` → 8001，无跨域
  - 请求三层架构：`api/request.js` axios 实例（baseURL/超时/拦截器）→ `api/chat.js` 接口封装（普通请求 axios、SSE 流式 fetch 生成器）→ 组件只调函数
  - pinia 状态管理（Options 写法：state/actions 分区），组件拆分 ChatHeader / MessageList / MessageBubble / ChatInput，App.vue 纯布局，组件直读 store 不传 props
- **kb-agent 宽度/深度分流问答**（见 kb-agent README「核心机制」）：宽度（列举/概览）走列表查询列「标题+关键词」，深度（具体细节）走向量检索 top3；维度判断纯 AI（只给当前问题 + 上一轮问题，不写词表不喂名单），拿不准偏向 depth；两种回答末尾带引导语形成纠正闭环
  - knowledge 表新增 keywords 字段（启动时自动迁移补列，旧数据零丢失）；上传时 LLM 顺带生成关键词；存量数据 `backfill_keywords.py` 补录
- **修复 SSE 透传丢换行 bug**：`iter_lines()` 剥换行未补回 + 空行被过滤 → 前端按 `\n\n` 切块永远切不出来，流式界面永远「思考中…」；修复为逐行补 `\n` 保留空行
- 新增「快速启动」章节：三个服务的依赖安装、配置、启动命令与端口约定

#### 2026年08月25日【企业知识库Agent·分块优化与流式输出】

- 表格独立分块：split_chunks 识别表格行（含 `|` 行 + 短行邻接传染），表格单元整体独立成块；"电费怎么收费"从进不了 top3 提升到 top1 命中收费表
- 上传统一调 LLM：手动指定 kind 时也自动生成 category（优先级：表单 > LLM > 未分类），一次上传仍只调一次 LLM
- 新增流式问答 `POST /api/v1/agent/chat/stream`（SSE）：meta → delta → done，前端可做打字机效果；llm_service 新增 stream_chat 生成器
- api 层提取 prepare_answer/plan_answer 公共逻辑；删除无调用者的 chat_with_context、pick_template
- 根目录重构：tricore 系列归档至 `archived/`，企业AI助手多 Agent 集群规划启动
- 创建 ai-assistant 占位目录（未来独立调度服务）；多 Agent 集群架构与实施路径记录于 README「统一调度设计」

#### 2026年08月24日【企业知识库Agent·上传解析与分类优化】

- 修复 docx 解析漏表格：原 parse_docx 只读段落，制度文件里的收费标准表、申请单表全部丢失；改为按文档顺序遍历底层 XML 块（段落+表格交替），表格转文本（每行一条、| 分隔），合并单元格重复文字去重
- 补录宿舍管理细则（记录 24）：内容 3851 → 4249 字，向量重同步 12 块；检索验证"住宿申请单怎么填"直接命中表格块
- 上传自动生成 category：classify_upload 新增 category 输出（AI 根据内容自由拟定，≤10 字，不设固定名单）；表单传了 category 用表单的，没传用 LLM 生成的
- 已知限制：Word 自动编号（"第X条"的 X）python-docx 拿不到；表格与其他内容挤在同一分块时检索排序可能偏低（后续可考虑表格独立分块）
- README 同步更新（upload 接口 category 说明）
- 合并校验：docx"段落-表格-段落"结构解析验证通过；端到端上传识别 category 正常（健身房使用管理办法 → 行政后勤管理）；无旧表旧接口残留引用

#### 2026年08月20日【企业知识库Agent·检索性能优化】

- 检索提速：向量检索从"Python 逐条循环"改为 NumPy 矩阵运算（纯计算部分加速约 500 倍）
- 向量归一化入库：把"除以模长"提前到索引时算好，查询时点积 = 余弦相似度（结果与原来完全一致）
- 向量存储从 JSON 文本改为 float32 二进制 BLOB（空间省 4 倍，读取零解析成本）
- 基准测试发现原方案最大瓶颈是 json.loads 解析（1 万条约 3 秒），二进制存储后消除
- 知识向量表重建（删旧表 → 新结构 → 重建索引），新旧算法检索结果验证一致
- 索引自动同步：增删改知识自动同步向量，reindex 降级为兜底工具
- 文档分块（Chunking）：长内容切 400 字/块、块间重叠 50 字，每块一个向量
- 移除 `POST /api/v1/policy/ask`（老式单字匹配问答），问答统一走 `/api/v1/agent/chat`

#### 2026年08月20日【企业知识库Agent·架构重构：统一知识表 v0.2】

- 三表合一：policies / documents / workflow_templates(+steps) → knowledge 统一表，kind 字段区分类型；流程步骤退化为 steps_json JSON 字段
- 接口合并：三组 16 个接口 → 一组 7 个（/api/v1/knowledge），删除旧接口和旧模型文件
- 新增统一上传：文件（txt/md/pdf/docx）→ 解析（pypdf/python-docx）→ LLM 识别分类（JSON 结构化输出，失败降级 document）→ 入库 → 自动分块向量化，立即可问答
- 上传支持手动指定 kind 覆盖 LLM 判断；非法文件类型拒绝
- 端到端验证通过：txt→制度、docx→流程（自动提取4步）、pdf→文档、手动覆盖、CRUD、reindex、exe 拒绝
- 历史说明与转变原因/收益记录在 README「架构演进」章节
- 调研 DeepSeek V4 Flash vs Pro：RAG 问答场景差异极小，保持 Flash 使用

#### 2026年08月14日【企业知识库Agent·快速迭代】

- FastAPI 起步教学：从 Hello World 到路由拆分、Pydantic 请求体、依赖注入（Depends）
- 接入 SQLite + SQLAlchemy ORM，三大模块（制度/文档/流程）完成 CRUD，流程模板支持级联删除
- 接入 DeepSeek 大模型，实现 RAG 问答（检索→增强→生成）
- 多轮对话记忆：messages 表存历史，session_id 延续上下文，检索时拼接上一轮问题
- 检索升级：单字匹配 → 字符打分排序 → 向量语义检索（硅基流动 bge-m3），未配置时自动降级
- 新增统一问答入口 `POST /api/v1/agent/chat`（智能问答分组），全局检索制度+文档+流程模板
- 流程模板纳入向量索引（步骤拼进内容一起向量化）
- 新增 `POST /api/v1/document/reindex` 重建向量索引接口
- 补上 .gitignore（.env、数据库文件、缓存不入库）；.env.example 中的真实 key 已清理

#### 2026年08月11日【切换：企业知识库Agent】

- 在当前的项目目录下，我打算做一个企业知识库Agent，使用编程语言python和fastAPI来进行实现主要能完成制度查询、文档问答、流程助手，当前先把框架搭好，你先把搭框架，要建立的架构内容给我看一下。

- 我目前处于一个学习阶段，这么多内容我难以一时消化，可以从最简单的地方，一点一点带我成长吗？

#### 2026年08月06日【tricore·前端与AI】

- 这个快照相关的信息，在前端项目上，通过一个左侧菜单栏的一个界面，体现出来，能够对这些快照信息进行管理（包括查看、删除、恢复）

- 后端项目的结构重新归纳一下，将相关模块的文件放在一个文件夹下，主要是model下的文件；另外前端的接口管理，将请求头的配置和创建与每个接口的请求分离，配置和创建用一个文件来进行，其他的接口，根据左侧菜单的顶部界面，把相关的接口都归类到一个文件下进行管理，放在`services`下

- 这个AI助手在分析中的时候，可不可以把它的推理内容也显示出来，让这个弹窗的回复内容丰富一些；另外这个AI助手不再提供放大界面框的功能，提供新开一个单独的界面，专门显示AI助手的对话框

  - 这个全屏对话框不够独立，要脱离现有的布局界面
  - 全屏对话框，无法向下滑动，修复一下
  - 我问这样的问题，怎么会直接失败？"现在公司的库存情况、销售情况如何，有多少员工？"
  - 这个分析过程能够在分析中的时候逐个显示吗？然后不放在结果的下面进行显示
  - 分析过程要展开，当结果出来后，结果要在最下面，分析过程在结果的上面

- AI管理，现在AI只有基本配置，我想要创建一个AI能力扩展界面，用于接入外部的MCP服务；另外对于使用项目中的规章制度、使用github上的代码仓库，你有什么好的建议吗？

- AI管理，这个MCP服务作为一个子界面放在AI管理下面进行进入，不和AI配置杂糅在一起，另外这个AI管理下的界面框UI优化一下，我没有看见padding值生效

#### 2026年07月29日【tricore·AI对话】

- AI对话，现在这个AI对话助手接了什么内容可以使用？

  - 就是让这个AI对话助手能够读取到数据库的所有信息，以方便它进行数据分析、总结和归纳
  - 接下来，我想让这个AI对话助手能够使用后端的接口，完成我对话时交办的任务，它可以继续向我提问需要哪些必要的信息
  - 我通过AI帮我生成的订单`ORD-20260729-006651`，并没有关联对应的配送司机，而且编辑的时候，订单数量和优惠方式都没有带出来，我想的是之前的逻辑是不是需要重新设计一下？
  - 现在出现了`❌ AI 对话达到最大轮次限制`，目前才进行了一轮完整的对话，这个问题修复一下

- 目前对于数据库数据，我想使用后端服务做到这个，能够在服务运行的时候，隔一段时间，保存并记录一下当前的数据信息，提供时间说明，最多保存十二条，出现第十三条的时候，就把时间线最老的给移除掉，再装入最新的数据记录，用于进行数据恢复，对系统的数据做一个保护

#### 2026年07月28日【tricore·销售库存与审批】

- 销售管理，订单列表界面，详情和编辑功能，要能够显示创建订单时，商品使用的每个仓库的下定数量，同时编辑功能里，要能够带出当时的下定数量，方便调整

- 库存管理，库存列表，水的总库存数不对，这是之前调试的时候出现的错误，你把它的数量修正一下

- 销售管理，订单列表界面，编辑功能，创建订单时使用的车辆司机信息也要带出，不能忽略

- 销售管理，订单列表界面，编辑功能，创建订单时使用的折扣优惠信息也要带出，不能忽略

  - 不使用这个巧妙的办法，把后端结构完善一下吧，保证带出的优惠信息和创建时一致

- 左侧主菜单栏，新增一个路由连接，名称叫做规章制度，支持上传文件，并形成一个文件列表，支持查看文件、删除文件、查询文件（文件名查询、文件分类查询），提供一个创建文件的按钮，在创建文件时，填写文件名称、文件分类、文件备注，再提供文件上传的按钮，支持上传文件。文件分类点击的时候弹出一个模态框，选择一个文件类型，支持自定义添加、删除文件分类，只需要输入名称即可添加，保存在数据库里，而且支持文件分类名称的关键字筛选。

- 规章制度，文件名关键字输入框UI设计有点奇怪，外部有了边框，怎么还要在框里面在设计一个输入框，和填写文件使用的输入框保持一致就行了；查看功能无法生效，被提示`未登录或令牌已失效`，能不能在系统内部预览而不是打开一个新的链接访问；在上传文件模块框里，模态框再宽一些，另外把分类管理的按钮放在上传文件的文件分类标题的下面，位置修改一下

  - 这套UI组件没有文件上传和文件预览的组件吗？另外这个模态框显示问题，在上传文件模态框里打开分类管理模态框时，分类管理模态框被上传文件模态框覆盖了，点击后看不见，修复一下；另外移除掉规章制度列表界面上的分类管理按钮
  - 规章制度，列表的查询功能，只选择分类的情况下查询失败，修复一下；
  - 更改后，我选择了一个文件分类上传，结果得到了这个`{"message":"error returned from database: 操作符不存在: uuid = text","success":false}`

- 办公协同，审批流程列表，创建流程里描述使用文本域展示；列表新增一个流程架构配置，用于添加常用流程，包括设置流程名称、流程描述以及审核步骤，可以保存多个架构并形成一个架构列表，每个架构项可以编辑和删除；在新建流程的模态框里，提供使用流程架构的按钮，点击后选择已有的流程架构，一键将标题、描述、步骤代入到新建审批流程中使用。

- 办公协同，审批流程列表，审批步骤按顺序进行，要第一个审批通过后，才能进行第二个人的审批，如果中途有人审批操作选择退回了，提供两个选项，一个是完全退回，另一个是本结点退回（意思就是退回给申请人进行重新审批时，之前通过的审批不变，直达这个被退回的结点进行流程审批）

  - 我在流程进行退回的时候，选择本结点退回，结果我在重新提交后，仍然要从第一个人开始审核，我的意思是，如果选择了本结点退回，那么之前通过的审核在进行重新提交后，就不需要审核了，直接显示审核通过

- AI工作，我是想通过对话的方式实现销售管理、办公协同、库存管理、规章制度的使用、分析、信息管理，在当下的AI发展情况下，结合我现在系统的情况，你有什么比较好的实现方案来进行？

  - 先做一个AI对话助手吧，你在左侧菜单栏的位置新增一个AI管理的可以下拉的菜单项，在那里生成一个配置这个AI对话助手的界面配合起来使用吧，先实现方案一
  - 补充，你刚才生成的AI方案信息放在项目的`README.md`文件里，标识好是在哪个区块

- AI管理，AI配置界面，它的位置居中显示吧，放在左侧看起来不和谐

  - 模型的选择怎么没有Deepseek v4？注意我现在使用的就是DeepSeek的模型
  - 这个AI对话界面通过浮动按钮触发吧，弹出一个小的界面框，在里面通过和AI对话实现业务场景，并移除AI管理下AI对话的路由链接而且AI管理下的AI配置界面保留
  - 不，下拉菜单后，选择AI配置的界面，不是直接点击AI管理界面触发
  - 我使用这个AI助手，得到了这个错误，修复一下：`响应解析失败: error decoding response body`
  - 这个AI助手界面不能上下滑动，里面的内容也不能复制，修复一下
  - 这个AI助手界面支持界面放大，而且能够正确显示md格式的信息内容

- 工作台界面，根据现在已有的项目情况完善一下，可以使用图表，让主界面更加美观、完善、充满UI设计美感

- 开发总结，在项目的`README.md`文件里填写今天的更新日志吧。

#### 2026年07月23日【tricore·订单库存与迁移】

- 销售管理，创建订单界面，订单商品的数量不能超过库存上限，如果为0，那么该商品不可被选择；优惠合计的计算，存在多个优惠项的情况下，应该是将商品总额进行了前一项的优惠之后，再拿到这个优惠后的价格，进行下一项的优惠计算，补充并修复一下。

- 库存管理，库存调整界面，重新设计一下，这里应该是添加仓库，对应每一个仓库项有一个库存数量，可以调整仓库的数量，可以移除仓库，根据仓库的数量的变化来判断是出库还是入库还是消库，调整好后，应该有一处文本域可以填写备注信息，关联给入库单、出库单；库存界面的列表库存数量对应总的库存数量，入库界面、出库界面的列表要有详情界面，显示相关的详情信息。

- 库存管理，库存调整界面，点击添加仓库，生成的仓库项，要支持下拉框选择仓库，否则用户不能一次就选择中需要的仓库，调整一下。

- 后端服务，目前同步更新数据库结构的方式如何？好不好？有没有其他更好的方案？

- 后端服务，数据迁移实现之后，我之前添加数据出现了问题，能不能把之前有的迁移数据清除掉，保留好今天我生成的数据

- 后端服务，不对不对，我之前有的三个仓库不见了、生成的四个用户数据也不见了、库存的数据也不对，应该是水、精钢、大水这样，却变成了毫无关系的另外八项库存，入库数据、出库数据、问题数据均不对，订单列表的数据不对、商品管理的数据也不对，全部同步成了以前迁移的旧数据，修复一下，还原成最新的数据，并撤回上一次的更改

- 后端服务，请完全重新设计一下这个迁移服务，简直是灾难，管理员账号并没有生成，我无法进入系统了

- 销售管理，订单列表界面，详情界面应该显示完整的信息，这里还是偏概括性了；编辑界面要完整显示创建订单模态框时生成的信息，并保存进行更改；创建订单模态框，注意现在每件商品在不同的仓库里面有库存数量，这里不能简单的填写商品数量，而要对应商品的每一个有库存数量的仓库进行商品数量的填写，汇总每个商品的商品数量，同步也要更新库存管理的库存界面的商品列表的总库存数量以及对应每一个商品的每一个库存里的数量。

- 补充，销售管理的订单完成创建后，同步也要更新库存管理的库存界面的商品列表的总库存数量以及对应每一个商品的每一个库存里的数量。

- 销售管理，订单列表界面，详情界面的UI设计得清晰明了一些，突出基本信息和商品信息，另外在这里也要看得见商品在不同仓库的发货数量，UI设计要清晰明确，结构舒适优美

- 销售管理，订单列表界面，编辑界面打开时保持和创建订单一致，返回得到的数据也与当时创建订单时一致，调整一下

- 开发总结，在项目的`README.md`文件里填写今天的更新日志吧。

#### 2026年07月22日【tricore·用户登录与库存】

- 我在本地电脑上面配置好了postgres服务器，连接的URL叫做：`postgres://postgres:123456@localhost:5432/tricore`，因为目前还在开发和调试系统，放在本地调试速度要好一些，后端rust项目上原来连接的数据库暂存，使用现在这个新的数据库

- 目前前端项目有两个，现在只在`tricore-web-rust`项目里进行前端开发，另外一个前端项目暂时不使用，在基础数据界面在新增一个用户管理，可以创建用户，包括用户名、密码、联系电话、部门、职位等等内容，同时系统新增一个登录界面，需要员工通过登录才能进入界面，注意主界面右上角的个人内容显示以及工作台界面员工数量的变化，其它的情况自行筛查一下。

- 登录界面，使用管理员账号登录失败，修复一下，另外登录界面的UI好好设计一下，突出轻柔、淡雅、舒适的风格

- 基础数据，用户管理界面的工号支持自动生成、部门关联部门管理的数据、职位关联部门下的职位数据

- 问题修复，进入前端界面读取数据发现`"error returned from database: 关系 \"某某表\" 不存在"`，能不能在后端启动的时候，判断必要的表是否已经生成，如果没有生成先自动生成

- 销售管理，商品管理界面的SKU支持自动生成；再新增一个商品分类管理界面，支持增删改；最后在新增商品的模态框里关联这些商品分类数据，这个商品分类表也是必要的表

- 库存管理，进行库存调整时，被提示"没有可用的仓库"，这个库存调整模态框再新增需要选择仓库吧，选择仓库关联仓库数据，仓库数据放在基础数据里面进行管理，名称叫做仓库管理，支持增删改，这个仓库表也是必要的表。

- 销售管理，创建订单模态框，客户关联客户管理的数据，配送车辆和司机关联车辆管理的数据，这里支持折扣优惠和金钱优惠（直接减免多少钱）

- 销售管理，创建订单模态框，这个模态框UI再宽一些，给填写数量、单价的组件再宽一些，另外单价不可更改，只是显示，数量不做上限限制，优惠方式可以支持叠加，就是用添加的方式，添加优惠信息，商品总额经过所有的优惠方式后，得到最终的实付金额，可以为每一个优惠方式添加简短的备注信息。

- 销售管理，如果订单已取消，那么库存的数量就不需要扣减，这些状态：待处理、已取消、已确认、处理中、已发货、已交付里，不需要后续再填写车辆信息

- 库存管理，入库界面的表单项入库单号给的列宽再多一些，至少保证能一行显示；问题界面，登记问题进行提交的时候被提示`Json deserialize error: UUID parsing failed: invalid length: found 0 at line 1 column 144`，修复一下

- 库存管理，问题界面，解决操作的输入框使用组件UI的文本域进行展示；另外库存、入库、出库、问题界面的表格列宽显得太紧实了，改变得宽松一些

- 开发总结，在项目的`README.md`文件里填写今天的更新日志吧，然后该更新、调整的地方也对应调整和更新。
